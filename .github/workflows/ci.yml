name: CI

on:
  push:
    branches: [main]
    paths:
      - 'stripe-mcp/**'
  pull_request:
    branches: [main]
    paths:
      - 'stripe-mcp/**'

defaults:
  run:
    working-directory: stripe-mcp

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20.x, 22.x]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: stripe-mcp/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run build
      - run: npm test
